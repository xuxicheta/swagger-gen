import { SwaggerDefinitions } from '../../types/swagger';

export const stubDefinitions = (): SwaggerDefinitions => ({
  one: {
    properties: {},
    description: '',
    required: [],
  },
  two: {
    properties: {},
    description: '',
    required: [],
  },
});
